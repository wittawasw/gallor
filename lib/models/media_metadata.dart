part of '../main.dart';

class MediaMetadata {
  final int? width;
  final int? height;
  final Duration? duration;
  final int? pageCount;

  const MediaMetadata({this.width, this.height, this.duration, this.pageCount});
}
